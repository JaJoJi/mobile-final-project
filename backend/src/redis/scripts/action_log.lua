-- action_log.lua
-- Placeholder. Real append-only action log logic lands in P0-BE-06.
-- KEYS[1] = match action log list key (e.g. match:<id>:actions)
-- ARGV[1] = action payload (json string)
-- Returns 1 on success.
return 1
