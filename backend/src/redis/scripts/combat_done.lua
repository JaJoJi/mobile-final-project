-- combat_done.lua
-- Placeholder. Real ack-collection logic lands in P0-BE-06.
-- KEYS[1] = match combat-acks hash key (e.g. match:<id>:combat_acks)
-- ARGV[1] = user id acking
-- ARGV[2] = ack payload (json string)
-- Returns 1 if both players have now acked, 0 if still waiting.
return 1
